name: flutter_reader
description: "A Flutter reader/book viewer module for embedding in other projects."
version: 0.1.0
homepage: https://github.com/user/flutter_reader

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/bg/

<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- release 需要显式声明 INTERNET 权限(debug/profile manifest 自带, main 没有)。
         不加会导致 release APK 无网络权限, 拉书章节正文时报 SocketException。 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:label="flutter_reader_example"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Android 11+(API 30) 包可见性: 应用必须显式声明要查询的 intent action,
         否则系统会隐藏对应服务/Activity, 相关功能静默失败(不抛异常)。
         参考 https://developer.android.com/training/package-visibility -->
    <queries>
        <!-- TTS 引擎服务。朗读功能依赖: 缺这条 TextToSpeech 绑定会永远不完成,
             报 "not bound to TTS engine", getDefaultEngine 返回 null。
             对齐原生 legado AndroidManifest.xml 的同款 queries 声明。 -->
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
        <!-- 用于 Flutter 引擎的 ProcessTextPlugin(文本选择菜单)。 -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>

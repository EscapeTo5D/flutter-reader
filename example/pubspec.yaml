name: flutter_reader_example
description: Example app for flutter_reader package.
publish_to: 'none'

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  flutter_reader:
    path: ../
  http: ^1.2.0
  # 持久化演示用(桌面端 sqflite 需 ffi)
  sqflite_common_ffi: ^2.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
